import { environment } from "environments/environment";

export const NOTICIA_CARROUSEL_NAV_DEF = {
  id: 'noticiaCarrousel',
  translateKey: 'noticiaCarrousel_nav_def',
  url: '/' + environment.URL_NOTICIA_CARROUSEL
};
